# 电子产品产测上位机软件平台开发方案

---

## 一、项目概述

### 1.1 设计目标

| 目标 | 说明 |
|------|------|
| **柔性调度** | 借鉴PLC梯形图扫描机制，测试项通过前置条件驱动，动态依赖、条件分支、自动并发；以事件驱动为主、定时扫描为辅，实现毫秒级响应。 |
| **全脚本化与跨产品复用** | 每个测试项封装为独立Python脚本，遵循标准输入/输出协议，跨产品共享、版本管理、热更新。 |
| **流程控制完备** | 支持串行、并行、条件分支、循环、互斥等待、子序列嵌套，但控制流均由`preconditions`声明，调度器无硬编码控制节点。 |
| **完全开源** | 零商业授权费用，全Python技术栈。 |
| **轻量化部署** | 最小生产部署约9核/16GB（不含端侧工位）。 |
| **生产稳定性** | 超时熔断、进程隔离、离线容灾、断点续传。 |
| **AI赋能** | DeepAgents + Qdrant RAG 实现需求识别与序列自动生成。 |
| **可视化编排** | AntV X6 3.x + Vue 3 + TypeScript，以依赖连线代替流程块。 |

### 1.2 原方案 vs 优化方案关键变化

- 所有步骤均为原子脚本节点，控制流由 `preconditions` 字段定义
- 调度器基于事件总线，扫描式评估就绪条件，动态调度 |
- 测试逻辑封装为独立脚本，通过脚本库直接复用
- 通过 `resources` 声明，调度器自动管理锁
- 事件驱动为主 + 定时兜底扫描，实时响应

---

## 二、总体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                    编排与展示层（云侧）                              │
│   AntV X6 3.x + Vue 3 + Element Plus + Monaco Editor               │
│   节点：脚本步骤、变量定义、循环容器、依赖连线                        │
├─────────────────────────────────────────────────────────────────────┤
│                    云侧服务层（云侧）                                │
│   FastAPI + PostgreSQL + Qdrant + NATS Server + 步骤脚本库管理      │
├─────────────────────────────────────────────────────────────────────┤
│               调度与执行层（端侧/云侧协同）                           │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  事件驱动扫描式调度引擎 (Event-Driven Scanner Scheduler)     │   │
│   │  · 事件总线 (EventBus)                                       │   │
│   │  · 步骤注册表 (StepRegistry) + 依赖索引                      │   │
│   │  · 前置条件评估器 (ConditionEvaluator)                       │   │
│   │  · 就绪队列 (ReadyQueue)                                     │   │
│   │  · 执行器池 (ProcessPoolExecutor)                            │   │
│   │  · 变量空间管理 (VariableSpace)                              │   │
│   │  · 资源锁管理器 (ResourceManager)                            │   │
│   │  · 循环/分支编译器 (LoopCompiler)                            │   │
│   │  · 故障域隔离 (FaultDomain)                                  │   │
│   └─────────────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────────┤
│              硬件抽象与执行层（端侧）                                 │
│   脚本可调用：PyVISA-py 或 OpenHTF Phase 插件          │
└─────────────────────────────────────────────────────────────────────┘
```

**调度核心循环**：
1. 事件驱动：步骤完成、变量变更、资源释放等事件触发受影响步骤的条件评估，满足即提交。
2. 定时兜底：低频全量扫描（1秒），防止事件丢失或外部状态依赖。

---

## 三、完整技术选型（合并与优化）

### 3.1 端侧

| 模块 | 组件 | 版本/说明 | License |
|------|------|-----------|---------|
| 操作系统 | Ubuntu 24.04 LTS / Deepin UOS | 官方推荐 | — |
| 容器运行时 | Docker | 最新稳定版 | Apache 2.0 |
| Python运行时 | Python 3.12 | 核心开发语言 | — |
| 硬件测试框架 | OpenHTF (可选) | v1.5.2+，作为脚本插件 | Apache 2.0 |
| 仪器通信 | PyVISA-py | 最新稳定版 | MIT |
| 序列调度 | **自研事件驱动扫描式调度器** | asyncio + multiprocessing + 事件总线 | 自研开源 |
| 表达式求值 | simpleeval + 内置函数库 | 最新稳定版 | MIT |
| 条件表达式引擎 | **自定义 ConditionEvaluator** | 支持步骤状态、变量、时间、资源 | 自研 |
| 事件总线 | **asyncio.Queue + 订阅模式** | 轻量实现 | — |
| 脚本执行隔离 | **multiprocessing.Process** | 子进程执行用户脚本 | Python内置 |
| 资源锁管理 | threading.Lock + 可选 Redis | 本地/分布式 | MIT/BSD |
| YAML解析 | PyYAML | 最新稳定版 | MIT |
| 云边通信 | nats-py | 最新稳定版 | Apache 2.0 |
| 本地缓存 | SQLite | 3.x（WAL模式） | Public Domain |

### 3.2 云侧

| 模块 | 组件 | 版本/说明 | License |
|------|------|-----------|---------|
| Web框架 | FastAPI | 最新稳定版 | MIT |
| ORM | SQLAlchemy 2.x | 异步支持 | MIT |
| 关系数据库 | PostgreSQL | 16.x+ | PostgreSQL License |
| 向量数据库 | **Qdrant** | v1.18.0+ | Apache 2.0 |
| AI框架 | DeepAgents | 最新稳定版 | MIT |
| 大语言模型 | DeepSeek / Qwen | 开源模型 | 开源 |
| Embedding | BAAI/bge-m3 | 最新稳定版 | MIT |
| 消息中间件 | NATS Server | v2.12.0+ | Apache 2.0 |
| 任务队列 | NATS JetStream | 内置 | Apache 2.0 |
| 脚本文件存储 | **本地文件系统 / NFS** | 脚本元数据存DB，文件存磁盘 | — |

### 3.3 前端（AntV X6 3.x）

| 模块 | 组件 | 版本/说明 | License |
|------|------|-----------|---------|
| 前端框架 | **Vue 3** | ^3.4.0 | MIT |
| 语言 | **TypeScript** | ^5.0.0 | — |
| 构建工具 | **Vite** | ^5.0.0 | MIT |
| **图编辑引擎** | **@antv/x6** | **^3.0.0** | MIT |
| **Vue 3 节点适配器** | **@antv/x6-vue-shape** | **^3.0.0** | MIT |
| 状态管理 | Pinia | ^2.0.0 | MIT |
| UI组件库 | Element Plus | 最新稳定版 | MIT |
| 代码编辑器 | Monaco Editor | 最新稳定版 | MIT |

> **⚠️ 重要**：X6 3.x 将所有插件（`@antv/x6-plugin-*`、工具包、几何包）合并到主包 `@antv/x6` 中统一导出。**无需单独安装任何插件包**。

### 3.4 Qdrant选型理由（保持不变）

| 对比维度 | Qdrant | PGVector |
|----------|--------|----------|
| p95延迟 | **36.73ms** | 60.42ms（**优39%**） |
| QPS（10并发）| **1,245** | 318（**优291%**） |
| 独立扩展性 | 可独立水平扩展 | 受限于PostgreSQL |
| Rust实现 | 内存安全、高性能 | — |

---

## 四、前端可视化编排器详细设计（AntV X6 3.x 适配依赖连线模式）

### 4.1 依赖安装

```json
{
  "dependencies": {
    "@antv/x6": "^3.0.0",
    "@antv/x6-vue-shape": "^3.0.0"
  }
}
```

### 4.2 目录结构

```
frontend/
├── src/
│   ├── views/
│   │   └── SequenceEditor/
│   │       ├── index.vue                 # 主编辑器入口
│   │       ├── components/
│   │       │   ├── GraphContainer.vue    # X6 画布容器
│   │       │   ├── StepLibraryPanel.vue  # 左侧步骤库面板（显示可用脚本）
│   │       │   ├── PropertyPanel.vue     # 右侧属性面板（编辑步骤参数、条件等）
│   │       │   ├── Toolbar.vue           # 顶部工具栏
│   │       │   └── nodes/                # 自定义 Vue 节点组件
│   │       │       ├── ScriptStepNode.vue       # 脚本步骤节点
│   │       │       ├── VariableNode.vue         # 变量定义节点（序列全局变量）
│   │       │       └── LoopContainerNode.vue    # 循环容器节点（语法糖，可展开）
│   │       ├── composables/
│   │       │   ├── useGraph.ts           # Graph 实例管理与生命周期
│   │       │   ├── useNodeRegistry.ts    # 节点注册（注册新节点类型）
│   │       │   ├── useDnd.ts             # 拖拽功能（从步骤库拖入脚本）
│   │       │   ├── useSerializer.ts      # X6 ↔ YAML（新版DSL）双向转换
│   │       │   └── useDependencyCheck.ts # 循环依赖检测等
│   │       ├── models/
│   │       │   ├── FlowChart.ts
│   │       │   └── nodes/
│   │       │       ├── CustomNode.ts
│   │       │       └── CustomEdge.ts
│   │       └── types/
│   │           └── node.d.ts
│   ├── main.ts                           # 全局注册 Vue 节点
│   └── App.vue
├── tsconfig.json
└── vite.config.ts
```

### 4.3 核心初始化代码

#### 4.3.1 main.ts —— 全局注册 Vue 节点

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import { register } from '@antv/x6-vue-shape'
import ScriptStepNode from './views/SequenceEditor/components/nodes/ScriptStepNode.vue'
import VariableNode from './views/SequenceEditor/components/nodes/VariableNode.vue'
import LoopContainerNode from './views/SequenceEditor/components/nodes/LoopContainerNode.vue'

const app = createApp(App)

register({
  shape: 'script-step-node',
  width: 180,
  height: 80,
  component: ScriptStepNode,
})
register({
  shape: 'variable-node',
  width: 200,
  height: 120,
  component: VariableNode,
})
register({
  shape: 'loop-container-node',
  width: 240,
  height: 160,
  component: LoopContainerNode,
})

app.use(createPinia())
app.mount('#app')
```

#### 4.3.2 App.vue —— TeleportContainer 必需

```vue
<template>
  <div id="app">
    <SequenceEditor />
    <TeleportContainer />
  </div>
</template>
<script setup lang="ts">
import { getTeleport } from '@antv/x6-vue-shape'
import SequenceEditor from './views/SequenceEditor/index.vue'
const TeleportContainer = getTeleport()
</script>
```

#### 4.3.3 useGraph.ts —— Graph 实例管理（适配依赖连线）

```typescript
import { ref, onMounted, onBeforeUnmount } from 'vue'
import { Graph, Scroller, Selection, Keyboard, History, Clipboard, Snapline, MiniMap } from '@antv/x6'

export function useGraph(containerId: string) {
  const graph = ref<Graph | null>(null)
  const container = ref<HTMLElement | null>(null)

  const initGraph = () => {
    container.value = document.getElementById(containerId)
    if (!container.value) return

    graph.value = new Graph({
      container: container.value,
      width: container.value.clientWidth,
      height: container.value.clientHeight,
      grid: { size: 10, visible: true },
      panning: { enabled: true, modifiers: 'shift' },
      mousewheel: { enabled: true, modifiers: 'ctrl', minScale: 0.5, maxScale: 2.0 },
      connecting: {
        snap: { radius: 20 },
        allowBlank: false,
        allowLoop: false,          // 防止自依赖
        allowMulti: true,          // 允许多条依赖连线
        router: 'manhattan',
        connector: { name: 'rounded', args: { radius: 8 } },
        validateConnection({ sourceCell, targetCell }) {
          // 禁止连接自身，并调用循环依赖检测
          return sourceCell.id !== targetCell.id && !wouldCreateCycle(graph.value!, sourceCell, targetCell)
        }
      },
      highlighting: {
        default: {
          name: 'stroke',
          args: { padding: 4, attrs: { 'stroke-width': 2, stroke: '#409EFF' } }
        }
      }
    })

    graph.value.use(new Scroller({ pageWidth: 2000, pageHeight: 2000, virtual: true }))
    graph.value.use(new Selection({ rubberband: true, multiple: true }))
    graph.value.use(new Keyboard({ enabled: true, global: true }))
    graph.value.use(new History({ enabled: true }))
    graph.value.use(new Clipboard())
    graph.value.use(new Snapline({ enabled: true, sharp: true }))
    graph.value.use(new MiniMap({ container: document.getElementById('minimap-container')!, width: 200, height: 150, padding: 10 }))

    const keyboard = graph.value.getPlugin('keyboard') as Keyboard
    keyboard.on('keydown', (e: KeyboardEvent) => {
      if ((e.metaKey || e.ctrlKey) && e.key === 'z') {
        e.preventDefault(); (graph.value?.getPlugin('history') as History)?.undo()
      }
      if ((e.metaKey || e.ctrlKey) && e.key === 'y') {
        e.preventDefault(); (graph.value?.getPlugin('history') as History)?.redo()
      }
    })

    return graph.value
  }

  const disposeGraph = () => { if (graph.value) { graph.value.dispose(); graph.value = null } }
  onMounted(initGraph)
  onBeforeUnmount(disposeGraph)
  return { graph, container, initGraph, disposeGraph }
}
```

#### 4.3.4 useNodeRegistry.ts —— 节点工厂

```typescript
import { Graph } from '@antv/x6'
import { v4 as uuidv4 } from 'uuid'

export enum NodeType {
  SCRIPT_STEP = 'script-step-node',
  VARIABLE = 'variable-node',
  LOOP_CONTAINER = 'loop-container-node'
}

export function createScriptStepNode(graph: Graph, scriptMeta: any, x: number, y: number) {
  return graph.addNode({
    shape: NodeType.SCRIPT_STEP,
    x, y,
    data: {
      stepId: uuidv4(),
      scriptName: scriptMeta.name,
      scriptVersion: scriptMeta.version,
      params: {},
      preconditions: [],
      resources: scriptMeta.resource_needs || [],
      timeout: 60,
      onFail: 'stop',
      exportOutputs: true,
    }
  })
}

export function createVariableNode(graph: Graph, x: number, y: number) {
  return graph.addNode({
    shape: NodeType.VARIABLE,
    x, y,
    data: { variables: {} }
  })
}
```

#### 4.3.5 useSerializer.ts —— X6 ↔ YAML（新DSL）双向转换

```typescript
import { Graph } from '@antv/x6'
import yaml from 'js-yaml'

export interface YamlStep {
  id: string
  script: string
  params?: Record<string, any>
  preconditions?: any
  resources?: string[]
  timeout?: number
  retry?: number
  on_fail?: string
  export_outputs?: boolean
  type?: string
  child?: any
}

export interface YamlPlan {
  name: string
  version: string
  scope?: { variables?: Record<string, any>; resources?: string[] }
  max_concurrency?: number
  steps: YamlStep[]
}

export function graphToYaml(graph: Graph): string {
  const nodes = graph.getNodes()
  const edges = graph.getEdges()
  const steps: YamlStep[] = []
  const variables: Record<string, any> = {}

  nodes.forEach(node => {
    const data = node.getData()
    if (node.shape === 'script-step-node') {
      // 收集所有入边，构建 preconditions
      const incomingEdges = edges.filter(e => e.getTargetCellId() === node.id)
      const preconditions = incomingEdges.map(edge => {
        const sourceId = edge.getSourceCellId()
        const sourceNode = nodes.find(n => n.id === sourceId)
        if (sourceNode?.shape === 'script-step-node') {
          const condData = edge.getData()?.condition || {}
          return { step: sourceNode.getData().stepId, ...condData }
        }
        return null
      }).filter(Boolean)

      steps.push({
        id: data.stepId,
        script: data.scriptName,
        params: data.params,
        preconditions: preconditions.length > 0 ? preconditions : undefined,
        resources: data.resources?.length > 0 ? data.resources : undefined,
        timeout: data.timeout,
        retry: data.retry,
        on_fail: data.onFail,
        export_outputs: data.exportOutputs,
      })
    } else if (node.shape === 'variable-node') {
      Object.assign(variables, data.variables)
    }
    // 循环容器节点处理略，简化处理
  })

  const plan: YamlPlan = {
    name: 'untitled',
    version: '3.0',
    scope: { variables },
    max_concurrency: 4,
    steps
  }
  return yaml.dump(plan, { indent: 2 })
}

export function yamlToGraphData(yamlStr: string): { nodes: any[]; edges: any[] } {
  const plan = yaml.load(yamlStr) as YamlPlan
  const nodes: any[] = []
  const edges: any[] = []
  const stepIdToNodeId: Record<string, string> = {}

  // 添加变量节点（如果存在）
  if (plan.scope?.variables && Object.keys(plan.scope.variables).length > 0) {
    const varNodeId = 'variables-root'
    nodes.push({ id: varNodeId, shape: 'variable-node', x: 50, y: 50, data: { variables: plan.scope.variables } })
  }

  plan.steps.forEach((step, index) => {
    const nodeId = `node-${step.id}`
    stepIdToNodeId[step.id] = nodeId
    nodes.push({
      id: nodeId,
      shape: 'script-step-node',
      x: 100 + (index % 3) * 250,
      y: 150 + Math.floor(index / 3) * 150,
      data: {
        stepId: step.id,
        scriptName: step.script,
        params: step.params || {},
        preconditions: step.preconditions || [],
        resources: step.resources || [],
        timeout: step.timeout || 60,
        onFail: step.on_fail || 'stop',
        exportOutputs: step.export_outputs ?? true,
      }
    })
  })

  // 根据 preconditions 创建边
  plan.steps.forEach(step => {
    if (step.preconditions) {
      const conds = Array.isArray(step.preconditions) ? step.preconditions : [step.preconditions]
      conds.forEach(cond => {
        if (cond.step) {
          const sourceId = stepIdToNodeId[cond.step]
          const targetId = stepIdToNodeId[step.id]
          if (sourceId && targetId) {
            edges.push({
              source: sourceId,
              target: targetId,
              data: { condition: { status: cond.status || 'passed' } }
            })
          }
        }
      })
    }
  })

  return { nodes, edges }
}
```

### 4.4 自定义 Vue 节点组件示例

**ScriptStepNode.vue**

```vue
<template>
  <div class="script-step-node" :class="{ 'has-error': hasError }">
    <div class="node-header">
      <span class="node-icon">⚙️</span>
      <span class="node-label">{{ data.scriptName || 'Script' }}</span>
    </div>
    <div class="node-body">
      <span class="node-id">{{ data.stepId }}</span>
    </div>
    <div class="node-status" :class="statusClass">{{ statusText }}</div>
  </div>
</template>

<script setup lang="ts">
import { computed, inject } from 'vue'
const getNode = inject('getNode') as () => any
const node = getNode?.()
const data = node?.getData() || {}
const hasError = computed(() => data?.status === 'error')
const statusText = computed(() => data?.status || 'idle')
const statusClass = computed(() => ({
  'status-idle': data?.status === 'idle' || !data?.status,
  'status-running': data?.status === 'running',
  'status-passed': data?.status === 'passed',
  'status-failed': data?.status === 'failed',
  'status-error': data?.status === 'error',
}))
</script>

<style scoped>
.script-step-node {
  width: 180px; min-height: 70px; background: #ffffff; border: 2px solid #409EFF;
  border-radius: 8px; padding: 8px 12px; cursor: pointer; box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  transition: all 0.2s; position: relative;
}
.script-step-node:hover { box-shadow: 0 4px 16px rgba(64, 158, 255, 0.25); }
.script-step-node.has-error { border-color: #F56C6C; }
.node-header { display: flex; align-items: center; gap: 6px; }
.node-icon { font-size: 16px; }
.node-label { font-weight: 600; font-size: 13px; color: #303133; }
.node-body { font-size: 12px; color: #909399; margin-top: 2px; }
.node-status { position: absolute; right: 8px; top: 8px; font-size: 11px; padding: 2px 8px; border-radius: 10px; }
.status-idle { background: #F4F4F5; color: #909399; }
.status-running { background: #E6F1FF; color: #409EFF; animation: pulse 1s infinite; }
.status-passed { background: #F0F9EB; color: #67C23A; }
.status-failed { background: #FEF0F0; color: #F56C6C; }
.status-error { background: #FDF6EC; color: #E6A23C; }
@keyframes pulse { 0%,100% { opacity:1; } 50% { opacity:0.5; } }
</style>
```

### 4.5 StepLibraryPanel 组件

- 从后端 API 获取脚本库列表（名称、版本、描述、所需资源）
- 每个脚本项可拖拽到画布，调用 `useDnd` 创建 `ScriptStepNode`
- 支持按标签/名称搜索

### 4.6 PropertyPanel 组件

根据选中节点类型动态显示表单：
- **ScriptStepNode**：编辑步骤ID、脚本名（只读）、输入参数表、前置条件高级编辑器（Monaco JSON）、超时、失败策略、资源列表、导出输出等。
- **VariableNode**：键值对表格。
- **LoopContainerNode**：循环模式、次数/条件、内部步骤预览。

### 4.7 X6 3.x 迁移要点

| 变更项 | 2.x 写法 | 3.x 写法 |
|--------|----------|----------|
| 插件导入 | `import { Scroller } from '@antv/x6-plugin-scroller'` | `import { Scroller } from '@antv/x6'` |
| 工具包导入 | `import { Dom } from '@antv/x6-common'` | `import { Dom } from '@antv/x6'` |
| 几何包导入 | `import { Point } from '@antv/x6-geometry'` | `import { Point } from '@antv/x6'` |
| Vue Shape | `@antv/x6-vue-shape@^2.x` | `@antv/x6-vue-shape@^3.x` |
| 动画 API | `node.transition(...)` | `node.animate(...)` |
| panning 默认 | `enabled: false` | `enabled: true` |
| 虚拟渲染 | 不支持 | `Scroller` 中 `virtual: true` |

---

## 五、测试序列定义语言（YAML DSL v3.0）

### 5.1 设计原则

- 所有测试步骤均为原子脚本节点，不区分 `sequential`、`parallel` 等容器。
- 控制流通过 `preconditions` 字段声明，调度器根据条件动态决定执行顺序和并行度。
- 高级结构（循环、子序列）作为语法糖，在编译阶段展开为基本步骤 + 条件。

### 5.2 步骤通用属性

| 属性 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | string | 是 | 步骤唯一标识 |
| `script` | string | 是 | 脚本相对路径或注册名 |
| `params` | dict | 否 | 输入参数，支持 `${scope.var}` 等变量引用 |
| `preconditions` | Condition | 否 | 前置条件表达式，默认无条件直接执行 |
| `resources` | list[string] | 否 | 所需仪器资源 ID 列表 |
| `timeout` | int | 否 | 超时秒数，默认 60 |
| `retry` | int | 否 | 失败重试次数，默认 0 |
| `on_fail` | string | 否 | 失败处理策略：`stop`、`skip`、`ignore` |
| `export_outputs` | bool | 否 | 是否将输出自动提升为全局变量（`步骤ID.输出key`） |

### 5.3 前置条件规范

条件对象支持简单/组合形式：

```yaml
# 简单条件：依赖某个步骤的状态
preconditions:
  - step: power_on
    status: passed   # passed, failed, any, skipped

# 逻辑组合
preconditions:
  all:
    - step: calibrate
      status: passed
    - expression: "${scope.temperature} < 40"
    - resource_available: "RF_SHIELD_BOX"
  timeout: 300       # 等待超时秒数，超时后步骤状态置为 skipped

# 任意条件满足即执行
preconditions:
  any:
    - step: optionA
      status: passed
    - step: optionB
      status: passed
```

**内置条件函数**：
- `step.<id>.status` 或 `step.<id>.outputs.<key>`
- `expression: "<simpleeval表达式>"` 访问 `scope`、`global`、`steps` 变量
- `resource_available: "<资源ID>"`
- `time_since('<step_id>') > <seconds>`
- 可通过 Python 注册自定义评估函数。

### 5.4 序列级定义

```yaml
name: "序列名称"
version: "3.0"
scope:
  variables:        # 序列局部变量
    voltage: 3.3
  resources:        # 全局硬件资源列表（可选）
    - DMM_CH1
    - RF_SHIELD_BOX
max_concurrency: 4  # 最大并行步骤数
```

### 5.5 完整示例

```yaml
name: "智能手机主板全检"
version: "3.0"
scope:
  variables:
    voltage_threshold: 3.3
    retry_count: 2
max_concurrency: 6

steps:
  - id: power_on
    script: "lib_common/dut_power_on.py"
    params:
      input_voltage: "${scope.voltage_threshold}"
    timeout: 60
    on_fail: stop
    export_outputs: true

  - id: rf_cal
    script: "lib_rf/calibrate.py"
    params:
      ref_power: "${power_on.measured_v}"
    preconditions:
      - step: power_on
        status: passed
    resources: ["RF_SHIELD_BOX"]

  - id: rf_power
    script: "lib_rf/power_meas.py"
    params:
      limit_min: 18
      limit_max: 22
    preconditions:
      all:
        - step: rf_cal
          status: passed
        - expression: "${global.temperature} < 50"
    resources: ["RF_SHIELD_BOX"]

  - id: stress_test
    type: loop            # 语法糖，编译时展开
    mode: for
    count: 50
    parallel: false
    child:
      script: "lib_common/stress_iter.py"
      params: {}
      preconditions:
        - step: "__previous__"   # 占位符，运行时替换为上一次迭代步骤ID
          status: passed

  - id: final_measure
    script: "lib_common/final_voltage.py"
    preconditions:
      all:
        - step: rf_power
          status: passed
        - step: stress_test
          status: passed     # 等待循环整体完成
    resources: ["DMM_CH1"]
```

---

## 六、调度层核心设计：事件驱动扫描式调度引擎

### 6.1 事件总线（EventBus）

采用 `asyncio.Queue` 和订阅模式，轻量高效。

```python
import asyncio
from enum import Enum
from collections import defaultdict

class EventType(Enum):
    STEP_STATUS_CHANGED = "step_status"
    VARIABLE_CHANGED = "variable"
    RESOURCE_RELEASED = "resource_released"
    TIMER_EXPIRED = "timer"
    EXTERNAL_CMD = "external"

class EventBus:
    def __init__(self):
        self._subscribers = defaultdict(set)
        self._queue = asyncio.Queue()

    def subscribe(self, event_type: EventType, callback):
        self._subscribers[event_type].add(callback)

    async def publish(self, event_type: EventType, data: dict):
        await self._queue.put((event_type, data))

    async def start(self):
        while True:
            event_type, data = await self._queue.get()
            for cb in self._subscribers.get(event_type, []):
                asyncio.create_task(cb(data))
            # 通用事件（None）用于全局兜底评估
            for cb in self._subscribers.get(None, []):
                asyncio.create_task(cb(data))
```

### 6.2 核心组件

| 组件 | 职责 |
|------|------|
| **StepRegistry** | 维护所有步骤定义和状态，提供**依赖索引**（步骤ID -> 依赖它的步骤ID集合，变量名 -> 引用该变量的步骤集合，资源 -> 等待该资源的步骤集合）。 |
| **ConditionEvaluator** | 解析并评估前置条件，利用 `VariableSpace` 和 `StepRegistry` 的状态。 |
| **ResourceManager** | 管理仪器资源锁，获取失败时返回 `False`，释放时发布 `RESOURCE_RELEASED` 事件。 |
| **ProcessPoolExecutor** | 执行子进程脚本，返回结果。 |
| **VariableSpace** | 分层变量存储，提供线程安全的读取和事件通知写入接口。 |
| **LoopCompiler** | 编译 `loop` 语法糖为基本步骤+条件，支持动态展开。 |

### 6.3 依赖索引维护

在步骤注册时建立：
- `step_dependents`: `{step_id: set(依赖该步骤的其他步骤ID)}`
- `variable_dependents`: `{var_name: set(包含该变量的步骤ID)}`
- `resource_waiters`: `{resource_id: set(声明该资源的步骤ID)}`

当事件发生时直接查找受影响集合，避免全量扫描。

### 6.4 调度器主循环（事件驱动 + 兜底扫描）

```python
class ScannerScheduler:
    def __init__(self, plan, variable_space):
        self.bus = EventBus()
        self.registry = StepRegistry()
        self.condition_eval = ConditionEvaluator(variable_space, self.registry, self.bus)
        self.executor = ProcessPoolExecutor(max_workers=plan.get('max_concurrency', 4))
        self.resource_mgr = ResourceManager(self.bus)
        self.variable_space = variable_space
        self.running = set()
        self.completed = set()
        # 绑定事件回调
        self.bus.subscribe(EventType.STEP_STATUS_CHANGED, self.on_step_status_changed)
        self.bus.subscribe(EventType.VARIABLE_CHANGED, self.on_variable_changed)
        self.bus.subscribe(EventType.RESOURCE_RELEASED, self.on_resource_released)
        # 全局兜底：任何事件触发增量评估
        self.bus.subscribe(None, self.trigger_dependency_eval)

    async def run(self):
        self.compile_plan(self.plan)
        asyncio.create_task(self.bus.start())
        asyncio.create_task(self.periodic_scan(1.0))

        while not self.is_finished():
            await asyncio.sleep(0.1)  # 主循环只负责等待结束，实际工作由事件驱动
        return self.generate_report()

    async def trigger_dependency_eval(self, data: dict):
        affected_ids = self.get_affected_steps(data) or self.registry.get_pending_ids()
        ready = self.scan_ready(affected_ids)
        for sid in ready:
            await self.try_submit(sid)

    async def try_submit(self, sid):
        if sid not in self.running and self.resource_mgr.acquire(sid):
            self.running.add(sid)
            asyncio.get_event_loop().run_in_executor(
                self.executor, self.execute_step, sid
            )

    def execute_step(self, step_id):
        # 执行子进程脚本，完成后通过跨进程队列发回结果
        ...
        # 调度器接收结果，发布事件
        self.bus.publish_sync(EventType.STEP_STATUS_CHANGED, {'step_id': step_id, 'status': result['status']})
        # 变量变更由 ContextProxy 内部触发

    async def on_step_status_changed(self, data):
        step_id = data['step_id']
        self.registry.update_status(step_id, data['status'])
        dependents = self.registry.get_step_dependents(step_id)
        ready = self.scan_ready(dependents)
        for sid in ready:
            await self.try_submit(sid)

    async def on_variable_changed(self, data):
        var_name = data['var_name']
        affected = self.registry.get_variable_dependents(var_name)
        ready = self.scan_ready(affected)
        for sid in ready:
            await self.try_submit(sid)

    async def on_resource_released(self, data):
        res_id = data['resource_id']
        waiters = self.registry.get_resource_waiters(res_id)
        ready = self.scan_ready(waiters)
        for sid in ready:
            await self.try_submit(sid)

    async def periodic_scan(self, interval):
        while True:
            await asyncio.sleep(interval)
            await self.trigger_dependency_eval({})
```

### 6.5 ContextProxy 与事件集成

脚本通过 `context` 可读取/写入变量，写入时自动发送 `VARIABLE_CHANGED` 事件（由调度器监听）。

```python
class ContextProxy:
    def __init__(self, step_id, variable_space, logger, nats, instrument, cache, event_queue):
        self.step_id = step_id
        self._variables = variable_space
        self._event_queue = event_queue  # 跨进程通信队列

    def set_var(self, name, value):
        self._variables.set(name, value)
        self._event_queue.put(('VARIABLE_CHANGED', {'var_name': name, 'value': value}))

    # 其他方法略...
```

### 6.6 资源锁管理

- 本地锁：`threading.Lock`，适用于单机。
- 分布式锁：Redis，适用于多工位。
- `acquire()` 非阻塞尝试获取，若失败步骤放回等待队列，待 `RESOURCE_RELEASED` 事件时重试。

---

## 七、测试项脚本标准化与数据交互协议

### 7.1 脚本接口规范

每个测试脚本为独立 `.py` 文件，必须包含：

```python
__author__ = "xxx"
__version__ = "1.0.0"
__description__ = "DUT power-on test"
__inputs__ = {
    "voltage": {"type": "float", "default": 5.0, "description": "Target voltage"}
}
__outputs__ = {
    "measured_v": {"type": "float", "description": "Measured voltage"}
}
__resource_needs__ = ["DMM_CH1"]  # 可选

def run(params: dict, context: dict) -> dict:
    """
    params: 合并后的输入参数字典
    context: 平台上下文对象 (ContextProxy)
    return: 输出字典，必须包含 'status' 字段
    """
    # 用户逻辑
    return {"status": "passed", "measured_v": 3.31}
```

### 7.2 数据交互方式

| 交互方式 | 说明 |
|----------|------|
| **输入参数** | 从 DSL 解析，支持 `${scope.xxx}`、`${steps.xxx.yyy}` 等变量替换 |
| **返回值** | 必须包含 `status`，其余输出按 `export_outputs` 规则注入变量空间 |
| **变量空间读写** | 通过 `context.get_var()` / `context.set_var()` 访问分层变量，写入受白名单控制 |
| **事件发布** | 通过 `context.publish_event()` 发布自定义消息（NATS），用于进度、日志等 |
| **仪器资源** | 脚本通过 `__resource_needs__` 声明，由调度器获取锁，脚本可通过 `context.instrument` 安全使用 |

### 7.3 ContextProxy 详细接口

```python
class ContextProxy:
    def get_var(self, name: str, default=None):
        """读取变量，支持作用域：global.xxx, scope.xxx, steps.xxx.yyy"""
    def set_var(self, name: str, value):
        """写入变量，仅允许 steps.<step_id>.* 或白名单全局变量"""
    def publish_event(self, event_type: str, payload: dict):
        """通过 NATS 发布事件到云侧"""
    def get_instrument(self, resource_name: str):
        """获取已锁定的 VISA 资源"""
    # 还包括 logger, nats 客户端, cache 等属性
```

---

## 八、OpenHTF 执行层（作为脚本插件保留）

OpenHTF 作为可选的测试执行方式，通过适配器脚本调用：

```python
# lib_openhtf/adapter.py
def run(params, context):
    import openhtf as htf
    test = htf.Test(test_name=params['test_name'])
    # 添加 measurements, plugs...
    record = test.execute()
    return {
        "status": record.outcome.name.lower(),
        "measurements": {m.name: m.measured_value for m in record.measurements}
    }
```

平台无需了解 OpenHTF，仅将其视为普通脚本。

---

## 九、端侧部署

### 9.1 端侧软件栈

| 层级 | 组件 | 版本 |
|------|------|------|
| 操作系统 | Ubuntu 24.04 LTS / Deepin UOS | — |
| 容器运行时 | Docker | 最新稳定版 |
| Python运行时 | Python 3.12 | — |
| 硬件测试框架 | OpenHTF (可选) | v1.5.2+ |
| 仪器通信 | PyVISA-py | 最新稳定版 |
| 序列调度 | 自研事件驱动扫描式调度器 | — |
| 云边通信 | nats-py | 最新稳定版 |
| 本地缓存 | SQLite（WAL模式） | 3.x |

### 9.2 SQLite 生产配置

```sql
PRAGMA journal_mode = WAL;
PRAGMA busy_timeout = 30000;
PRAGMA synchronous = NORMAL;
PRAGMA cache_size = -20000;
```

---

## 十、云侧服务

### 10.1 步骤脚本库管理 API

- `POST /api/scripts/upload`：上传脚本文件，解析元数据存入 PostgreSQL，文件存磁盘。
- `GET /api/scripts`：列表查询，支持标签、关键词。
- `GET /api/scripts/{id}`：获取脚本详情及内容。
- `PUT /api/scripts/{id}`：更新版本。
- `DELETE /api/scripts/{id}`：下架。

### 10.2 AI 辅助生成增强

由于采用依赖驱动模型，AI 只需预测步骤间的依赖关系，RAG 检索相似依赖模式，生成 DSL 更简单。

---

## 十一、云边通信

统一采用 **NATS**：

| 方向 | 内容 | NATS特性 |
|------|------|----------|
| 云→端 | 序列下发、执行指令 | Core |
| 端→云 | 进度、数据、日志 | JetStream（持久化） |
| 端→云 | 心跳、状态 | Core |

---

## 十二、临时文件存储

- 端侧存储：`/var/cache/test_platform/temp/`
- 通知机制：NATS `file.ready` 事件
- 清理策略：拉取成功即删除，失败 7*24 小时定时清理

---

## 十三、部署方案

### 13.1 最小化生产部署（<10工位）

| 组件 | 资源建议 |
|------|----------|
| NATS Server | 1核 / 1GB |
| PostgreSQL | 2核 / 4GB |
| Qdrant | 2核 / 4GB |
| VictoriaMetrics | 1核 / 2GB |
| Grafana | 1核 / 1GB |
| FastAPI + 调度服务 | 2核 / 4GB |

**总资源**：约 9核 / 16GB

### 13.2 规模化部署（>50工位）

| 组件 | 部署方式 |
|------|----------|
| NATS Server | 3节点集群 |
| PostgreSQL | 主从复制 |
| Qdrant | 集群模式 |
| FastAPI | 多实例 + 负载均衡 |

---

## 十四、方案优势总结

| 优势 | 说明 |
|------|------|
| **极致灵活性** | 事件驱动 + 条件扫描，任意动态依赖，自动并发，适应复杂流程 |
| **高复用性** | 独立脚本步骤，版本管理，跨产品共享 |
| **调度简单可靠** | 事件总线 + 依赖索引，毫秒响应，无复杂图遍历 |
| **硬件互斥透明** | 资源声明自动加锁，消除手动临界区错误 |
| **完全开源** | 零商业授权，全 Python 技术栈 |
| **强大可视化** | AntV X6 3.x 依赖连线直观，降低编排门槛 |
| **AI 原生** | DeepAgents + Qdrant 高性能检索，自动生成依赖声明 |

## 十五、风险与应对

| 风险 | 应对措施 |
|------|----------|
| 依赖关系复杂导致可视化混乱 | 提供依赖分析视图、自动分层布局、条件折叠 |
| 事件丢失或顺序错乱 | 兜底全量扫描 + 事件持久化日志 |


---

## 十六、实现状态跟踪（2026-08 更新）

> 本章节记录设计方案中各项功能的实际实现状态，与代码库保持同步。

### 16.1 核心引擎

| 功能 | 设计章节 | 实现状态 | 代码位置 | 备注 |
|------|----------|----------|----------|------|
| 事件驱动扫描式调度引擎 | 六 | ✅ 已实现 | `src/ate_platform/scheduler/scanner_scheduler.py` | EventBus + 依赖索引 + 增量评估 |
| 兜底定时扫描 | 六.4 | ✅ 已实现 | `src/ate_platform/scheduler/scanner_scheduler.py` | 1秒间隔全量扫描 |
| ContextProxy 变量空间 | 七.3 | ✅ 已实现 | `src/ate_platform/executor/context_proxy.py` | 分层变量、`@measure` 装饰器 |
| 资源锁管理 | 六.6 | ✅ 已实现 | `src/ate_platform/scheduler/` | 本地锁；分布式锁预留 Redis 接口 |
| LoopCompiler 语法糖展开 | 五.5 | ✅ 已实现 | DSL 编译阶段展开 loop/子序列 |
| 进程隔离执行 | 六.2 | ✅ 已实现 | `src/ate_platform/executor/` | multiprocessing.Process |
| YAML DSL v3.0 | 五 | ✅ 已实现 | `src/shared/dsl.py` | 步骤、前置条件、资源声明 |
| OpenHTF 适配器 | 八 | ✅ 已实现 | `src/ate_platform/openhtf/` | 作为可选脚本插件 |

### 16.2 云侧服务

| 功能 | 设计章节 | 实现状态 | 代码位置 | 备注 |
|------|----------|----------|----------|------|
| FastAPI Web 框架 | 十 | ✅ 已实现 | `src/ate_cloud/main.py` | 端口 8000，Swagger /docs |
| 脚本库管理 API | 十.1 | ✅ 已实现 | `src/ate_cloud/api/v1/scripts.py` | CRUD + 版本管理 + 内容读写 |
| AI 辅助脚本生成 | 十.2 | ✅ 已实现 | `src/ate_cloud/api/v1/scripts_generate.py` | POST /generate, /refine |
| NATS 云边通信 | 十一 | ✅ 已实现 | `src/ate_cloud/nats/`, `src/ate_platform/nats/` | Core + JetStream |
| SSE 实时事件推送 | — | ✅ 已实现 | `src/ate_cloud/nats/sse_bridge.py` | 执行事件实时流 |
| 脚本版本管理 | — | ✅ 已实现 | `src/ate_cloud/services/script_versioning.py` | Git-based 版本控制 |
| JWT 认证 | — | ✅ 已实现 | `src/ate_cloud/auth/` | RS256 算法，30分钟过期 |

### 16.3 前端可视化

| 功能 | 设计章节 | 实现状态 | 代码位置 | 备注 |
|------|----------|----------|----------|------|
| AntV X6 3.x 图编辑引擎 | 四.3 | ✅ 已实现 | `frontend/src/views/SequenceEditor/` | X6 3.1.7 |
| 脚本步骤节点 | 四.4 | ✅ 已实现 | `frontend/src/views/SequenceEditor/components/nodes/` | ScriptStepNode.vue |
| 属性面板 | 四.6 | ✅ 已实现 | PropertyPanel 组件 | 动态表单 |
| X6 ↔ YAML 双向转换 | 四.3.5 | ✅ 已实现 | `useSerializer.ts` composable | |
| 拖拽创建节点 | 四.3 | ✅ 已实现 | `useDnd.ts` composable | |
| 循环依赖检测 | 四.3.3 | ✅ 已实现 | `useDependencyCheck.ts` | validateConnection |
| Monaco 代码编辑器 | 三.3 | ✅ 已实现 | 脚本内容编辑 | |

### 16.4 超出原设计的增强功能

以下功能在原设计方案中未提及，但在开发过程中根据产线需求 added：

| 功能 | 实现状态 | 代码位置 | 说明 |
|------|----------|----------|------|
| **三层仿真系统** | ✅ 已实现 | `src/ate_platform/simulation/` | 驱动级 / DryRun / 全链路，支持噪声注入 |
| **MockDriverFactory** | ✅ 已实现 | `src/ate_platform/drivers/mock_factory.py` | DMM + PSU 虚拟仪器 |
| **AI 故障诊断 (RAG)** | ✅ 已实现 | `src/ate_cloud/services/failure_indexer.py` | Qdrant 向量检索 + Neo4j FMEA 知识图谱 |
| **SPC 统计过程控制** | ✅ 已实现 | `src/ate_cloud/api/v1/spc.py` | 控制图、报警（路由未挂载） |
| **校准管理** | ✅ 已实现 | `src/ate_cloud/api/v1/calibrations.py` | 校准记录、到期检查（路由未挂载） |
| **可追溯性** | ✅ 已实现 | `src/ate_cloud/api/v1/trace.py` | 序列号追溯（路由未挂载） |
| **多工位工作流** | ✅ 已实现 | `src/ate_cloud/api/v1/workflows.py` | 跨工位测试编排（路由未挂载） |
| **录制/回放** | ✅ 已实现 | `src/ate_cloud/api/v1/recordings.py` | 执行录制、回放、差异对比（路由未挂载） |
| **操作员检查点** | ✅ 已实现 | `src/ate_cloud/api/v1/operator_checkpoints.py` | 人工确认流程（路由未挂载） |
| **故障预测** | ✅ 已实现 | `src/ate_platform/scheduler/fault_predictor.py` | 基于历史数据的预测性维护 |
| **换型优化** | ✅ 已实现 | `src/ate_platform/scheduler/changeover_optimizer.py` | CP-SAT 求解器优化排产 |
| **人力资源分配** | ✅ 已实现 | `src/ate_platform/scheduler/` | 人机协同调度 |
| **自适应跳过** | ✅ 已实现 | `src/ate_platform/scheduler/` | 基于前置结果动态跳过测试项 |
| **ATML 报告导出** | ✅ 已实现 | `src/ate_cloud/api/v1/reports.py` | ATML XML 标准格式 |
| **gRPC 驱动接口** | ✅ 已实现 | `src/ate_platform/drivers/grpc/` | 远程仪器驱动 |
| **OpenTelemetry 可观测性** | ✅ 已实现 | `src/ate_cloud/observability/` | 分布式追踪 |
| **NATS Leafnode 边缘自治** | ✅ 已实现 | `config/nats-leafnode.conf` | 断网容灾 |
| **Alembic 数据库迁移** | ✅ 已实现 | `alembic/` | 12 个迁移版本 |
| **CI/CD 流水线** | ✅ 已实现 | `.github/workflows/` | lint + test + frontend + security |

### 16.5 API 路由挂载状态

当前 `router.py` 挂载了 10 个路由（47 个端点），另有 13 个路由文件已实现但**尚未挂载**：

**已挂载 (10)**: health, node_templates, scripts, scripts_generate, sequences, executions, changeover, dashboard, resources, reports

**未挂载 (13)**: auth, calibrations, debug, diagnose, faults, limits, operator_checkpoints, products, recordings, spc, trace, workers, workflows

> ⚠️ `limits.py` 和 `products.py` 未设置路由前缀，挂载前需添加 prefix。
> ⚠️ `operator_checkpoints`、`recordings` 与 `executions` 共享 `/executions` 前缀，需注意路由冲突。

### 16.6 部署状态

| 部署模式 | 状态 | 说明 |
|----------|------|------|
| Docker Compose (dev) | ✅ 可用 | 全栈本地开发：nats + qdrant + neo4j + ate-cloud + ate-platform |
| Docker Compose (cloud) | ✅ 可用 | 云侧部署：nats + qdrant + neo4j + ate-cloud |
| Podman Compose | ✅ 兼容 | 与 Docker Compose 命令兼容，已在 192.168.5.24 验证 |
| 虚拟设备仿真 | ✅ 可用 | `ATE_SIMULATION_MODE=true` 或 API 触发三层仿真 |

### 16.7 待完成项

| 项目 | 优先级 | 说明 |
|------|--------|------|
| 挂载剩余 13 个 API 路由 | 高 | 需先为 limits/products 添加前缀，处理 executions 前缀冲突 |
| F1-F7 验证检查项 | 中 | flexible-production-optimization 计划的最终验证尚未执行 |
| 生产环境 PostgreSQL 迁移 | 中 | 当前默认 SQLite，生产环境建议切换 PostgreSQL |
| 前端构建产物部署 | 中 | 当前仅 dev 模式，需配置 nginx 部署静态资源 |
| 真实硬件测试验证 | 低 | 当前仅虚拟设备仿真，真实 PyVISA 驱动未经产线验证 |


---
